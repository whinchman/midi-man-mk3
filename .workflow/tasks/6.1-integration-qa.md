# Task: integration QA

- **Type**: qa
- **Status**: pending
- **Repo**: engine
- **Parallel Group**: 6
- **Feature Branch**: feature/ui-refactor
- **Branch**: feature/ui-refactor/integration-qa
- **Base Branch**: feature/ui-refactor
- **Source Item**: #78
- **Dependencies**: 5.1-main-rs-cleanup.md

## Description

Verify the complete ui-refactor feature branch builds cleanly, all tests pass,
clippy is clean, and the 7-zone TUI layout is functionally correct. Write any
missing integration-level tests. File bugs for any regressions found.

This is a read-and-verify task — do not implement features. Write tests only
where coverage gaps exist that cannot be addressed by pointing back to the
implementing coder.

Files to touch: potentially `engine/src/` test modules (add integration tests
if missing); no application code changes.

**Build verification steps:**
1. `cargo test -p engine` — must pass all unit tests with zero failures
2. `cargo clippy -p engine --features hw-io -- -D warnings` — must produce zero warnings
3. `cargo build -p engine --features hw-io --release` — must succeed
4. `cargo build -p engine --release` (no hw-io) — must succeed (ensures no
   feature-gate leakage from always-compiled structs like `UiLocalSnapshot`)

**Functional verification (manual or via TestBackend tests):**

For each item, write a `#[test]` using `ratatui::backend::TestBackend` if the
behavior is not already covered by existing unit tests:

- [ ] Full 7-zone layout renders on an 80×24 terminal without panic
- [ ] `FocusPanel::Sequencer` border is `Rgb(0, 255, 255)` when focused
- [ ] `FocusPanel::SeqParams` border is `Rgb(0, 64, 64)` when not focused
- [ ] Title bar left contains the string "217 Industries"
- [ ] Title bar left "midi-man-mk3" span has style `fg(Rgb(255, 0, 255))`
- [ ] Transport bar contains `BPM`
- [ ] F3 panel SEED value renders as `0x` + uppercase hex digits
- [ ] CLI panel input line starts with `> `
- [ ] `BpmDelta` applied at extremes: verify clamp at 20 and 300 via `apply_command`
- [ ] `SeedSet(0xDEAD)` sets `rand_seed == 0xDEAD` and `rng_seed` reflects it
- [ ] `ChannelSet(1)` → `midi_channel == 0`; `ChannelSet(16)` → `midi_channel == 15`

**Regression checks:**

- Existing tests for `shift_action_key_to_command`, `select_port_idx`, and
  `root_key_to_command` must continue passing under their new behavior.
- If any test from the pre-refactor codebase was deleted without replacement,
  file it as a bug in `.workflow/BUGS.md` with severity and reproduction steps.

**Clippy lint categories to check manually:**
- No `#[allow(dead_code)]` left on removed variants
- No unused imports
- No `unwrap()` outside of `#[cfg(test)]` blocks

## Acceptance Criteria

- [ ] `cargo test -p engine` passes with zero failures
- [ ] `cargo clippy -p engine --features hw-io -- -D warnings` zero warnings
- [ ] `cargo build -p engine --features hw-io --release` succeeds
- [ ] `cargo build -p engine --release` succeeds
- [ ] All 7 layout zones render without panic on 80×24 `TestBackend`
- [ ] Title bar fuchsia span verified
- [ ] SEED hex format verified
- [ ] CLI `> ` prefix verified
- [ ] BPM clamp behavior verified (unit test)
- [ ] `SeedSet` dual-field update verified (unit test)
- [ ] `ChannelSet` 1-indexed conversion verified (unit test)
- [ ] No regression bugs filed (or bugs are filed with detail in `BUGS.md` if found)
- [ ] `rustfmt` applied to any new test files

## Interface Contracts

This task is read-only with respect to application code. It verifies the
contracts established by all previous tasks:

- `UiLocalSnapshot<'a>` from Task 3.1 is populated correctly by Task 4.1
- `render_frame` produces the 7-zone layout
- `run_ui` sends correct `InputCommand` and `MidiCtrlMsg` values
- `apply_command` handles `BpmDelta`, `SeedSet`, `ChannelSet`, `MidiDeviceName`
- `run_midi_out` loop processes `ctrl_rx` without blocking or panicking

## Context

- Run all tests on the merged `feature/ui-refactor` branch after Tasks 1.1–5.1 are merged in order.
- Use `ratatui::backend::TestBackend` for widget-level assertions — it captures rendered cell content and styles.
- TestBackend test pattern:
  ```rust
  let backend = TestBackend::new(80, 24);
  let mut terminal = Terminal::new(backend).expect("TestBackend terminal");
  terminal.draw(|frame| render_frame(frame, &state, &snapshot)).expect("draw");
  let buffer = terminal.backend().buffer().clone();
  // Assert on buffer.get(col, row).symbol() or .style()
  ```
- Code standards: Rust stable edition 2021, safe Rust only, no `unwrap()` in non-test code, clippy must pass, rustfmt.

## Notes

